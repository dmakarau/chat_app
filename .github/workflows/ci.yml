name: Flutter Development CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  # Code Quality and Testing
  test-and-analyze:
    name: Test & Code Quality
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.7'
        channel: 'stable'
        cache: true

    - name: Print Flutter Version
      run: flutter --version

    - name: Install Dependencies
      run: flutter pub get

    - name: Verify Dependencies
      run: flutter pub deps

    - name: Generate Code (Mocks, etc.)
      run: dart run build_runner build --delete-conflicting-outputs

    - name: Check Code Formatting
      run: dart format --output=none --set-exit-if-changed lib/models/ lib/services/ lib/screens/ lib/widgets/ lib/utils/ test/

    - name: Analyze Code (excluding Firebase config)
      run: flutter analyze --no-fatal-infos lib/models/ lib/services/ lib/screens/ lib/widgets/ lib/utils/ test/

    - name: Run Unit Tests
      run: flutter test test/unit/ --coverage

    - name: Run Widget Tests  
      run: flutter test test/widget/ --coverage

    - name: Run All Tests with Coverage
      run: flutter test --coverage

    - name: Upload Coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: coverage/lcov.info
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  # Documentation Generation
  documentation:
    name: Generate Documentation
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.7'
        channel: 'stable'
        cache: true

    - name: Install Dependencies
      run: flutter pub get

    - name: Generate Code Documentation
      run: dart doc --exclude firebase_options.dart

    - name: Deploy Documentation to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: success()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: doc/api

  # Security and Dependency Checks
  security-check:
    name: Security & Dependencies
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.7'
        channel: 'stable'
        cache: true

    - name: Install Dependencies
      run: flutter pub get

    - name: Check for Outdated Dependencies
      run: flutter pub outdated

    - name: Run Dependency Audit
      run: dart pub deps

    - name: Check for Security Issues
      continue-on-error: true
      run: |
        echo "🔍 Security Analysis:"
        # Check for common security issues (non-breaking)
        grep -r "http://" lib/ test/ || echo "✅ No insecure HTTP URLs found"
        grep -r "TODO.*security\|FIXME.*security\|hack\|temporary" lib/ test/ || echo "✅ No security-related TODOs found"
        echo "✅ Security check completed"

  # Final Status Check
  ci-status:
    name: CI Status
    runs-on: ubuntu-latest
    needs: [test-and-analyze, documentation, security-check]
    if: always()

    steps:
    - name: Check Results
      run: |
        if [[ "${{ needs.test-and-analyze.result }}" == "success" ]]; then
          echo "✅ Tests and Code Analysis: PASSED"
        else
          echo "❌ Tests and Code Analysis: FAILED"
          exit 1
        fi
        
        if [[ "${{ needs.security-check.result }}" == "success" ]]; then
          echo "✅ Security Check: PASSED"
        else
          echo "⚠️ Security Check: Issues found (non-blocking)"
        fi
        
        if [[ "${{ needs.documentation.result }}" == "success" || "${{ needs.documentation.result }}" == "skipped" ]]; then
          echo "✅ Documentation: OK"
        else
          echo "⚠️ Documentation: Issues found (non-blocking)"
        fi
        
        echo ""
        echo "🎉 Development CI Pipeline Complete!"
        echo "📊 Tests: ${{ needs.test-and-analyze.result }}"
        echo "🔍 Security: ${{ needs.security-check.result }}"
        echo "� Docs: ${{ needs.documentation.result }}"